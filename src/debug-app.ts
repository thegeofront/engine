// author: Jos Feenstra
// purpose: environment to test eyefinder functionalities

export function Run(canvas: HTMLCanvasElement, context: HTMLDivElement) {

    // setup an input 
    let input = new HTMLInputElement();
    // input.onchange = readFile;
    context.appendChild(input);

}

// function readFile(input: HTMLInputElement, ev: Event) {

//     if !input.files?

//     for (File file in input.files!) {
//         console.log(`File name: ${file.}`);
//         alert(`Last modified: ${file.lastModified}`);
//     }
// }
