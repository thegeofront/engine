import { GeonImage } from "./Image";

export class ImageProcessing {
    static gaussianBlur(image: GeonImage): GeonImage {
        return image;
    }

    static sobel(image: GeonImage): GeonImage {
        return image;
    }

    static canny(image: GeonImage): GeonImage {
        return image;
    }
}
