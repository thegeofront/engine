{
  "name": "geon-engine",
  "version": "0.5.1",
  "author": "Jos Feenstra",
  "license": "MIT",
  "description": "All-in-one 3D Geometry Engine",
  "main": "dist/lib.js",
  "types": "dist/lib.d.ts",
  "scripts": {
    "build": "rimraf tsconfig.tsbuildinfo && rimraf dist/ && tsc --build",
    "clean": "tsc --clean",
    "rimraf": "./node_modules/rimraf/bin.js",
    "prepack": "npm run build",
    "watch": "tsc --build --watch",
    "lib": "webpack --mode=development",
    "production": "webpack --mode=production",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "geon",
    "geometry",
    "3d",
    "engine",
    "geomatics"
  ],
  "devDependencies": {
    "ts-loader": ">=8.0.5",
    "typescript": "^4.3.5",
    "rimraf": "^3.0.1"
  },
  "dependencies": {}
}
