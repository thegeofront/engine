{
  "name": "geon-engine",
  "version": "0.1.5",
  "author": "Jos Feenstra",
  "license": "MIT",
  "description": "Lightweight 3d geometry library + 3d renderer",
  "main": "build/lib.js",
  "types": "build/lib.d.ts",
  "scripts": {
    "prepublish": "tsc --build",
    "watch": "tsc --build --watch",
    "build": "tsc --build",
    "lib": "webpack --mode=development",
    "production": "webpack --mode=production",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "geon",
    "geometry",
    "3d",
    "engine",
    "geomatics"
  ],
  "devDependencies": {
    "ts-loader": ">=8.0.5",
    "typescript": "^4.3.5"
  },
  "dependencies": {}
}
